name: Churn MLOps CI Pipeline

on:
  push:
    branches:[main]
  pull_request:
    branches:[main]

jobs:
  tests:
    runs-on:ubuntu-latest

    steps:
    -name: Code Checkout
     uses: actions/checkout@v3

    -name: Python 3.11 Setup
     uses: actions/setup-python@v4
     with:
      python-version: '3.11'

    -name: Libraries INstall
     run: |
      python -m pip install --upgrade pip
      pip instal -r requirements.txt

    -name: Data Download
     run: |
      python src/data/clean_data.python

    -name: Model Training
     run: